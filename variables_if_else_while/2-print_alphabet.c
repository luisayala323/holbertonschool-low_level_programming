#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Write a program that prints the alphabet in lowercase
 * Return: 0
 *
 */
int main(void)
{
	char alfa = 'a'

	while (alfa <= 'z')

	{
		putchar(alfa);
		alfa++;
	}
	putchar('\n');

	return (0);
}
