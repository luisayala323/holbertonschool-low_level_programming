#include <stdio.h>
/**
 * main - rints all single digit numbers base 10
 * Return: 0
 */
int main(void)
{
	int nume;

	for (nume = 0; nume < 10; nume++)
		printf("%d", nume);
	printf("\n");
	return (0);
}
